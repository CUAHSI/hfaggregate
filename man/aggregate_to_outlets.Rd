% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_to_outlets.R
\name{aggregate_to_outlets}
\alias{aggregate_to_outlets}
\title{Aggregate flowpaths and divides to outlet groups}
\usage{
aggregate_to_outlets(
  gpkg = NULL,
  vpu = NULL,
  flowpath = NULL,
  divide = NULL,
  crs = 5070,
  pois = NULL,
  outlets = NULL,
  outfile = NULL
)
}
\arguments{
\item{gpkg}{Path to a hydrofabric GeoPackage.}

\item{vpu}{Optional VPU code to subset (if supported by your readers). Not
currently used internally but kept for API compatibility.}

\item{flowpath}{Optional layer name for flowpaths in \code{gpkg}. If \code{NULL},
defaults used by \code{read_hydrofabric()}.}

\item{divide}{Optional layer name for divides in \code{gpkg}. If \code{NULL}, defaults
used by \code{read_hydrofabric()}.}

\item{crs}{Target CRS used when reading the hydrofabric. Default: \code{5070}.}

\item{pois}{An \code{sf} (or data frame) with a \code{flowpath_id} column indicating
POI-associated flowpaths to anchor mainstem levelpaths.}

\item{outlets}{Optional character/numeric vector of flowpath IDs to treat as
outlets when \code{pois} is \code{NULL} or when additional outlets should be seeded.}

\item{outfile}{Optional output path (reserved for future write support).}
}
\value{
A named list with two \code{sf} layers:
\describe{
\item{divides}{Polygons unioned by \code{group_id} (one per outlet group).}
\item{flowpaths}{Mainstem lines unioned by \code{group_id} with attributes:
\code{levelpathid}, \code{vpuid}, \code{member_comid} (CSV of members),
and \code{flowpath_toid} representing the collapsed next-group link.}
}
}
\description{
Builds outlet-based groups, unions divides by group, and unions mainstem
flowpaths by group to produce an outlet-scale network layer. This function
identifies critical junctions along levelpaths carrying points-of-interest
(POIs) and treats their immediate upstream siblings as additional outlets,
ensuring tributary groups are preserved where POIs branch.
}
