% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{prepare_network}
\alias{prepare_network}
\title{Prepare Hydrologic Network}
\usage{
prepare_network(network_list)
}
\arguments{
\item{network_list}{A list containing \code{sf} objects:
\itemize{
\item \code{flowpaths}: flowpath/flowline features with at least
\code{flowpath_id}, \code{flowpath_toid}.
\item \code{divides}: catchment/divide features (ideally with \code{divide_id}).
}}
}
\value{
The input list with updated \code{flowpaths} and \code{divides} \code{sf} objects.
}
\description{
Adds hydrosequence (\code{hydroseq}), geometric measures (area/length),
and total contributing drainage area (\code{tot_drainage_area}) to the
\code{flowpaths} element of a network list. Also standardizes geometry column
names, normalizes names to lowercase, and drops duplicates.
}
\details{
\itemize{
\item \code{tot_drainage_area} is computed by accumulating per-flowpath \code{areasqkm}
downstream; \code{NA} areas are treated as zero to enable accumulation.
\item The function expects a list with \code{flowpaths} and \code{divides} \code{sf} objects.
\item If duplicate rows or duplicate IDs are found, they are removed with a
warning.
}
}
